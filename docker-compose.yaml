services:
    app:
        build: .
        environment:
            - NODE_ENV=development
        ports:
            - "5173:5173"
        volumes:
            - .:/app
            - /app/node_modules
        command: yarn dev

volumes:
    node_modules:
